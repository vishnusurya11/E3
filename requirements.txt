# Core dependencies
pyyaml>=6.0
watchdog>=3.0.0
httpx>=0.24.0
websocket-client>=1.6.0
requests>=2.31.0

# Web framework
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
jinja2>=3.1.0

# AI/LLM dependencies
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-core>=0.2.0
pydantic>=2.0.0

# Image processing
Pillow>=10.0.0

# Database (using stdlib sqlite3, but adding sqlalchemy for ORM if needed)
# sqlalchemy>=2.0.0  # Optional - uncomment if using ORM

# Neo4j Graph Database
neo4j>=5.0.0
py2neo>=2021.2.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-mock>=3.11.0

# Code quality
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.5.0

# CLI
typer>=0.9.0
rich>=13.5.0  # For better CLI output

# Utilities
python-dotenv>=1.0.0  # For .env file support
beautifulsoup4>=4.12.0  # For HTML parsing
pydub>=0.25.0  # For audio processing and combining
pytz>=2023.3  # For timezone handling in gutenberg_cli.py

# YouTube API
tzdata>=2024.1  # zoneinfo timezone data for Windows (DST-aware schedule conversion)
google-api-python-client>=2.0.0  # YouTube Data API v3
google-auth-oauthlib>=1.0.0  # OAuth2 authentication for YouTube
google-auth-httplib2>=0.1.0  # HTTP transport for Google APIs

# SVG/PDF rendering (for AI stamp in generate_prompts.py)
svglib>=1.5.0
reportlab>=4.0.0

# Audio/TTS dependencies (optional — install separately with pip or uv)
# CPU:   pip install torch torchaudio
# CUDA:  pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu124
# After torch: pip install qwen_tts peft soundfile
soundfile>=0.12.0  # Audio file I/O (required by qwen_tts, no CUDA needed)

# Development and debugging
ipython>=8.0.0  # For interactive development